(function(define, undefined) {
    'use strict';
    define([
        'backbone'
    ], function(Backbone) {
        var StateAccountModel = Backbone.Model.extend({

        });
        return StateAccountModel;
    });
}).call(this, define || RequireJS.define);
